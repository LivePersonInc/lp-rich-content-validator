import * as compatibleRaw from './compatible';
import * as notCompatibleRaw from './notCompatible';

const compatible = Object.values(compatibleRaw);
const notCompatible = Object.values(notCompatibleRaw);

export { compatible, notCompatible };
